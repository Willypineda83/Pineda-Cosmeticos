
---
layout: base.njk
title: Videos – Pineda Cosméticos
---
<h1>Videos – Pineda Cosméticos</h1>
<p>Toca cualquier miniatura para ir directo a YouTube.</p>
<div class="grid">
{% for v in collections.videos | reverse %}
  <a class="card" href="{{ v.data.permalink | url }}" target="_blank" rel="noopener">
    <img class="thumb" src="{{ v.data.thumbnail }}" alt="{{ v.data.title }}">
    <div class="title">{{ v.data.title }}</div>
    {% if v.data.tag %}<div class="meta">{{ v.data.tag }}</div>{% endif %}
  </a>
{% endfor %}
</div>
<p class="footer">© Pineda Cosméticos</p>
